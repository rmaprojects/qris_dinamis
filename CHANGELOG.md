## 1.0.0

- Initial release
- `convertQris()` — Convert static QRIS string to dynamic QRIS string
- `convertQrisToImage()` — Generate dynamic QRIS as PNG image with template overlay
- `parseQris()` — Parse QRIS string and extract merchant data
- CRC-16/CCITT validation
